## Summary

Describe the problem and resulting behavior.

## Checklist

- [ ] Changed the generator rather than hand-editing generated output, where applicable.
- [ ] Ran `just lint`, `just test`, and `git diff --check` (or explained any limitation).
- [ ] Regenerated affected output and verified repeat-run stability, where applicable.
- [ ] Reviewed provenance, freshness, transaction, and credential-isolation implications.
- [ ] Signed off every commit with `git commit -s`.
